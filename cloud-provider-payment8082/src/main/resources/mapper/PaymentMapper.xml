<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.ybq87.springcloud.dao.IPaymentDao">
    <insert id="create" parameterType="top.ybq87.springcloud.entities.Payment" useGeneratedKeys="true" keyProperty="id">
        insert into payment(serial)
        values (#{serial,jdbcType=VARCHAR});
    </insert>

    <resultMap id="BaseResultMap" type="top.ybq87.springcloud.entities.Payment">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <id column="serial" property="serial" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="getPaymentById" resultMap="BaseResultMap" parameterType="Long">
        select *
        from payment
        where id = #{id,jdbcType=BIGINT};
    </select>

</mapper>